"""
The **enums** submodule contains enumerations that represent constants used throughout the Riot API wrapper.

These enums define values for regions, queue types, tiers, divisions, continents, and other fixed API parameters, ensuring consistent and type-safe usage across the package.

**Typical usage:**

    from pyke import Continent, Region, Tier

    print(Continent.EUROPE)
    print(Region.EUW)
    print(Tier.PLATINUM)
"""
